<p class="bold mt-4 mb-1">
	Program
</p>
{% if innslag.getProgram( monstring.monstring ).getAll()|length > 0 %}
	<p class="mt-0">{{ innslag.getNavn() }} er satt opp i følgende program-hendelser</p>
{% endif %}

<ol class="list-unstyled">
{% for forestilling in innslag.getProgram( monstring.monstring ).getAll() %}
	<li>
		Nr. {{ forestilling.getNummer( innslag ) }} i 
		<a href="{{ forestilling.getLink() }}#innslag_{{ innslag.getId() }}" target="_blank">{{ forestilling.getNavn() }}</a>
		(forestillingen starter {{ forestilling.getStart()|date('d.M H:i') }})
	</li>
{% else %}
	<li>{{ innslag.getNavn() }} er ikke satt opp i programmet</li>
{% endfor %}
</ol>